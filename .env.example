# ============================================
# Portfolio Tracker - Environment Variables
# ============================================
# Copia este archivo a .env y configura los valores

# ============================================
# PUERTOS - Generados por dev-tunnel
# ============================================
# Run: dev-tunnel env portfolio > .env.ports
# Los puertos se cargan desde .env.ports automáticamente
# NO editar manualmente - usar dev-tunnel

# ============================================
# SECRETOS DE APLICACIÓN
# ============================================
# Generar con: openssl rand -base64 32
JWT_SECRET=REEMPLAZAR_CON_SECRETO_ALEATORIO
# Opcional - duración del token (default: 1h)
JWT_EXPIRES_IN=1h

# ============================================
# BASE DE DATOS
# ============================================
# Generar contraseñas seguras en producción
POSTGRES_USER=portfolio_user
POSTGRES_PASSWORD=REEMPLAZAR_CON_PASSWORD_SEGURO
POSTGRES_DB=portfolio

# ============================================
# E2E TESTS
# ============================================
# Puerto para DB de tests (separada de desarrollo)
PORT_DB_TEST=10013
# URLs para Playwright
BASE_URL=http://localhost:10001
API_URL=http://localhost:10002

# ============================================
# CONEXIÓN DATABASE (para backend)
# ============================================
# La URL se construye automáticamente en docker-compose
# DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
