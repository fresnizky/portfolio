# Frontend Environment Variables for Local Development
# Used when running pnpm dev locally (outside Docker)

# API URL - Points to backend through dev-tunnel
VITE_API_URL=https://api.portfolio-dev.resnizky.ar/api
